# @package _global_

defaults:
  - /data: coco.yaml
  - /model: segmentation.yaml
  - /paths: default.yaml
  - /extras: default.yaml
  - /hydra: default.yaml
  - _self_
  - /experiment: null

task_name: "area_curv_stab"
tags: ["dev"]

# Calibrated checkpoint produced by `src/calibrate.py` (contains nc_curves)
ckpt_path: ???

data:
  eval_source: "trainval"
  eval_subset: "val"
  test_split: null
  trainval_split: "yuhi"
  num_workers: 4

fg_class_idx: 1
base_seg_threshold: 0.5
alpha_min: 0.0
alpha_max: 0.5
alpha_steps: 51
eta: 1.0e-6

w: 5
epsilon: 5.0e-3
rho: 0.2

tau_stab_sweep:
  enabled: false
  w_values: []
  epsilon_values: []
  rho_values: []

max_images: 50
